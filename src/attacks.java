public class attacks {
    String attackName;
    int attackPower;
    int attackStatus;

    public attacks(String name, int power, int status) {
        attackName = name;
        attackPower=power;
        attackStatus=status;
    }
}